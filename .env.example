OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4.1
OPENAI_CHAT_MODEL=gpt-4.1

# 家族で蓄積ボキャブラリーバンクを共有するための設定。
# Vercelダッシュボードで Storage → Create Database → Redis を作成して
# このプロジェクトに接続すると、REDIS_URL が自動で追加されます。
# ローカルで手動設定する必要はありません（Vercel側で自動的に設定されます）。
REDIS_URL=
