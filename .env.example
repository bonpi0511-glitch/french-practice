OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4.1
OPENAI_CHAT_MODEL=gpt-4.1

# 家族で蓄積ボキャブラリーバンクを共有するための設定。
# Vercelダッシュボードで Storage → Create Database → KV（Upstash for Redis）を
# 作成してこのプロジェクトに接続すると、以下の環境変数が自動で追加されます。
# ローカルで手動設定する必要はありません（Vercel側で自動的に設定されます）。
KV_REST_API_URL=
KV_REST_API_TOKEN=
